<?php

    // Incrementation
    $item = 0;
    // 1
    $item = $item + 1;
    // 2
    $item++;
    // 3
    $item += 1;

    echo "Incrementation de la variable Item = ". $item . " \n";

    // Opération de calcul
    $nbPersonne = 10;
    $total = ((10 + 1) - $nbPersonne) * 2;
    // 2
    echo " Total Nombre de personne = " . $total;




?>